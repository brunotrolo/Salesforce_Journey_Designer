# Engineering

Only `to-spec` and `implement` are vendored from this category of the upstream repo — see
the curation table in `.claude/skills/README.md` for why. The rest of `mattpocock/skills`'
`engineering/` category (`ask-matt`, `grill-with-docs`, `triage`, `improve-codebase-architecture`,
`setup-matt-pocock-skills`, `to-tickets`, `wayfinder`, `prototype`, `diagnosing-bugs`,
`research`, `tdd`, `domain-modeling`, `codebase-design`, `code-review`,
`resolving-merge-conflicts`, `wizard`) was not imported: no agent in this project cites it
by name.
